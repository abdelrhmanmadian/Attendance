# Some hosts set NODE_ENV=production before running `npm install`, which
# makes npm skip devDependencies by default — breaking the build, since
# typescript/vite/tsx/prisma (the CLI) all live there. Force them to always
# install regardless of NODE_ENV, on every platform this repo gets deployed to.
include=dev
